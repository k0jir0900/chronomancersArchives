# GENERAL
TZ=America/Santiago
# SECRET_KEY se autogenera en el arranque (entrypoint.sh) y persiste en el volumen app_secrets

# CERTIFICATE
DOMAIN=localhost
DAYS=826

# DB
# Root password is random per init (MYSQL_RANDOM_ROOT_PASSWORD in docker-compose); not used by the app.
DB_HOST=mysql
DB_PORT=3306
DB_NAME=chronomancers_archives
DB_USER=chronomancers_user
DB_PASSWORD=chronomancers_pass